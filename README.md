# ormf-3d-forecasting
High-performance service for real-time trajectory prediction using WOMD. Trains a GNN/Transformer, optimizes with TensorRT (INT8/FP16) for sub-20ms latency, and serves predictions via FastAPI. Feeds the Planning agent.
